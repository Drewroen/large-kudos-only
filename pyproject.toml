[project]
name = "strava-kudos-bot"
version = "0.1.0"
description = "Playwright bot that gives kudos on Strava's activity feed"
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.44,<2",
    "python-dotenv>=1.2.2",
]
